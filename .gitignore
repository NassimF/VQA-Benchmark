# Vector database (large, local only)
chroma_db/

# Literature review Excel files (binary, not needed for reproducibility)
literature-review/

# Raw video/audio files (large binaries — not committed)
data/raw/videos/*.mp4
data/raw/videos/*.m4a
data/raw/videos/*.mkv
data/raw/videos/*.webm
data/raw/videos/*.wav
data/raw/videos/*.mp3
data/raw/videos/*.part
# Caption/subtitle files ARE committed (small, needed for reproducibility)
# *.vtt files in data/raw/videos/ are tracked

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
dist/
build/

# Environment variables
.env
.env.*

# Jupyter checkpoints
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Large model caches
*.bin
*.safetensors
