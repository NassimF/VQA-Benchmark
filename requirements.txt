# Core dependencies
pyyaml>=6.0

# Embeddings & vector DB (Phase 5)
sentence-transformers>=2.7.0
chromadb>=0.5.0

# Frame captioning (Phase 4)
# torch and transformers pulled in by sentence-transformers
transformers>=4.45.0
Pillow>=10.0.0
opencv-python>=4.9.0
qwen-vl-utils>=0.0.8

# LLM generation (Phase 6)
openai>=1.30.0
anthropic>=0.28.0

# Evaluation (Phase 9)
numpy>=1.26.0
scipy>=1.13.0

# Testing
pytest>=8.0.0
